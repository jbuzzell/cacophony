#ifndef ARITHMETIC_H
#define ARITHMETIC_H

#include <vector>

int gcd(int a, int b);
int getNearest(int x, int y, int target);
int getNearestElement(std::vector<int> arr, int n, int target);
int lcm(int a, int b);

#endif // !ARITHMETIC_H#pragma once
