#ifndef ARITHMETIC_H
#define ARITHMETIC_H

#include <vector>

using namespace std;

int absVal(int x);
int gcd(int a, int b);
int getNearest(int x, int y, int target);
int getNearestElement(vector<int> arr, int n, int target);
int lcm(int a, int b);

#endif // !ARITHMETIC_H
